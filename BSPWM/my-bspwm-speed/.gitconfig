[user]
	email = wolfis_rain@mail.ru
	name = xxxvik-xakerxxx
[color]
	diff = auto
	status = auto
	branch = auto
	ui = true
[core]
	editor = subl3
[alias]
  lg = log --date=format:%c --format=format:'%C(bold blue)%h%C(reset) - %C(bold green)(%ad)%C(reset) %C(white)%s%C(reset) %C(dim white)- %an%C(reset)%C(bold yellow)%d%C(reset)' --all
